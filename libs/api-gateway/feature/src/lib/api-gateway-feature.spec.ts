import { apiGatewayFeature } from './api-gateway-feature';

describe('apiGatewayFeature', () => {
  it('should work', () => {
    expect(apiGatewayFeature()).toEqual('api-gateway-feature');
  });
});
