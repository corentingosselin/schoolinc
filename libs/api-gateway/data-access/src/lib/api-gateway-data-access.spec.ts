import { apiGatewayDataAccess } from './api-gateway-data-access';

describe('apiGatewayDataAccess', () => {
  it('should work', () => {
    expect(apiGatewayDataAccess()).toEqual('api-gateway-data-access');
  });
});
