import { backendAuthServiceCore } from './backend-auth-service-core';

describe('backendAuthServiceCore', () => {
  it('should work', () => {
    expect(backendAuthServiceCore()).toEqual('backend-auth-service-core');
  });
});
