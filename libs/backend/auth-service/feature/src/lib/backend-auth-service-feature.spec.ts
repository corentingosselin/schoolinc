import { backendAuthServiceFeature } from './backend-auth-service-feature';

describe('backendAuthServiceFeature', () => {
  it('should work', () => {
    expect(backendAuthServiceFeature()).toEqual('backend-auth-service-feature');
  });
});
