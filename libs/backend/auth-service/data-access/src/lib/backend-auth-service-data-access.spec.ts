import { backendAuthServiceDataAccess } from './backend-auth-service-data-access';

describe('backendAuthServiceDataAccess', () => {
  it('should work', () => {
    expect(backendAuthServiceDataAccess()).toEqual(
      'backend-auth-service-data-access'
    );
  });
});
