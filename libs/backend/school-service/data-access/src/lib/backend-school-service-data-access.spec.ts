import { backendSchoolServiceDataAccess } from './backend-school-service-data-access';

describe('backendSchoolServiceDataAccess', () => {
  it('should work', () => {
    expect(backendSchoolServiceDataAccess()).toEqual(
      'backend-school-service-data-access'
    );
  });
});
