import { backendSchoolServiceFeature } from './backend-school-service-feature';

describe('backendSchoolServiceFeature', () => {
  it('should work', () => {
    expect(backendSchoolServiceFeature()).toEqual(
      'backend-school-service-feature'
    );
  });
});
