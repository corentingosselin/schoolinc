import { backendSchoolServiceCore } from './backend-school-service-core';

describe('backendSchoolServiceCore', () => {
  it('should work', () => {
    expect(backendSchoolServiceCore()).toEqual('backend-school-service-core');
  });
});
