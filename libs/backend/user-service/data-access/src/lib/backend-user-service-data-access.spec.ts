import { backendUserServiceDataAccess } from './backend-user-service-data-access';

describe('backendUserServiceDataAccess', () => {
  it('should work', () => {
    expect(backendUserServiceDataAccess()).toEqual(
      'backend-user-service-data-access'
    );
  });
});
