import { backendUserServiceFeature } from './backend-user-service-feature';

describe('backendUserServiceFeature', () => {
  it('should work', () => {
    expect(backendUserServiceFeature()).toEqual('backend-user-service-feature');
  });
});
